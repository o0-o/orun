# STDOUT  Value found traversing positional arguments as fields, except
#         the last argument which is a JSON string or file
########################################################################

"${_script_wrapper[@]}"                 ||

return 1

return 0
